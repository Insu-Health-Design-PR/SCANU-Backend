"""Temporary adapters to the current Layer 8 implementation.

The first migration slice keeps hardware/runtime code in the existing
local `layer8_ui` package while the API surface is being split.
"""

